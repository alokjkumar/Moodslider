import { MoodFilterPipe } from './mood-filter.pipe';

describe('MoodFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MoodFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
